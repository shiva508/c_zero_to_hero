#include <stdio.h>

int main() {
    int a ;
    //int memory_address = &a; // Get the memory address of variable 'a'
    printf("Value of a: %d\n", a);
    printf("Value of &a: %d\n", &a);
    //printf("Value of a: %d\n", memory_address);
    return 0;
}